import Theme from "../Theme.js"

/**
 * High contrast theme.
 */
export default class HighContrastTheme extends Theme {
	/** @type {Object} */
	static color = {
		text: "#fff",
		background: "#000"
	}
}